# STATUS — spotify-angular

## Frontier
- **Current frontier:** **Audit + audit-fix pass complete (2026-07-13); build-verification by hand pending.** The whole guide was audited and the blocker + all majors were fixed (see Drift log): the `LikedIndex.clear()` forward-ref, the dead glossary anchors, the M8→M11 marker-system relocation, the M10/M11 authoritative-copy claims, the M6→M10 attribution, and the M5 gates; a confirmation re-audit passed with no residual blocker/major/warning. **All 12 milestones remain 📝 (touched ones flagged ⚠ re-verify) — not build-verified.** Next is the hands-on dev phase: run the app (needs a real Spotify app + allowlisted account; Premium for the M7/M8 playback gates) and walk each milestone's Done-when.

## Source inputs
| Input file | Used for (stack / scope / decisions) | Provided on | Re-checked on |
|------------|--------------------------------------|-------------|---------------|
| `EarthViewMusic/CLAUDE.md` | Architecture, conventions, data flow (authoritative summary) | 2026-07-10 | — |
| `EarthViewMusic/PLAN.md` | Original design only — **stale** (top-artists + IndexedDB); code overrides | 2026-07-10 | — |
| `EarthViewMusic/package.json` | Pinned dependency versions (seed for Verified stack) | 2026-07-10 | — |
| `EarthViewMusic/src/**` (whole tree) | The authoritative code — milestone ladder reverse-engineered from it | 2026-07-10 | — |

## Milestone status
| Milestone | Status | Verified on | Notes |
|-----------|--------|-------------|-------|
| M0 — Scaffold & tooling | 📝 ⚠ re-verify | — | Drafted: 15 steps + verify |
| M1 — Spotify login (PKCE) | 📝 ⚠ re-verify | — | Drafted: 10 steps + verify |
| M2 — HTTP resilience layer | 📝 ⚠ re-verify | — | Drafted: 11 steps + verify; adaptive AIMD gate |
| M3 — Liked Songs streaming | 📝 ⚠ re-verify | — | Drafted: 7 steps + verify; **2026-07-13: `LikedIndex.clear()` added here (blocker fix)** |
| M4 — The globe (three.js) | 📝 ⚠ re-verify | — | Drafted: 7 steps + verify; flight/heat deferred |
| M5 — Country resolution + heat 🚦 | 📝 ⚠ re-verify | — | Drafted: 10 steps + verify; **reality-check gate**; **2026-07-13: `clearData` guard added (blocker fix)** |
| M6 — Hover/select panel + fixups | 📝 ⚠ re-verify | — | Drafted: 11 steps + verify; REST Countries descoped (D7), heat-mode toggle added (D8); **2026-07-13: library-console refs retargeted M9→M10** |
| M7 — The live player | 📝 ⚠ re-verify | — | Drafted: 7 steps + verify; controls need Premium (R4) |
| M8 — Trip / Flight mode | 📝 ⚠ re-verify | — | Drafted: 12 steps + verify; flight isolation (D5); **2026-07-13: flight overlay trimmed to plane-only — marker system relocated to M11 (gold-plating fix)** |
| M9 — Explore controls & sync | 📝 ⚠ re-verify | — | Drafted: 14 steps + verify; boot-sync + Actions page; skipped orphaned globe-hud; **2026-07-13: `clearData` forward-ref resolved (blocker fix)** |
| M10 — Library console | 📝 ⚠ re-verify | — | Drafted: 17 steps + verify; reversible applyChange + ISRC dedup; **2026-07-13: step 05 reframed — `clear` moved to M3 (blocker fix)** |
| M11 — Settings, data transfer & polish | 📝 ⚠ re-verify | — | Drafted: 10 steps + verify; **MarkerKind born here + the flight-layer marker engine grows here (step 03)**; S2/S3/S4 polish |

<!-- Status key: ✅ verified (Done-when passed by hand) · 📝 drafted (guide written, NOT build-verified) · ⏳ in progress · ❌ not started -->

## Drift log
| Date | Where | Guide said | Reality is | Action taken |
|------|-------|-----------|-----------|--------------|
| 2026-07-13 | Full re-audit fix pass (M0–M2, M4–M9, M11 + foundation/stack) | (a) M5/06 linked decision-log `#d5-…-triexplore-…`; (b) M9/01 "Create `playlist.ts`" + listed it "New" in M9 | (a) the D5 heading slugifies to `…-tripexplore-…` (every other file spells it right) so the link was dead; (b) `playlist.ts`/`Playlist`/`snapshotId` already exist since M7 — M9's block was a byte-dup | Fixed the `triexplore`→`tripexplore` anchor; dropped `playlist.ts` from M9/01's create list + bundle banner (3→2 files) + M9/00 "New" + rendered it as "unchanged since M7" in M9/15. Plus 6 WARNING + ~18 MINOR + 3 clarify-step pedagogy passes (see Session log 2026-07-13). Touched milestones stay ⚠ re-verify. |
| 2026-07-13 | Re-audit follow-up (M6 ×12, M7/00, M11/00, M5/08) | — | — | Fixed real defects the re-audit surfaced: stripped leaked tool-call artifacts (`</content>` / `</invoke>`) from the tail of **all 12 M6 files** (visible junk, drafting leftover); M7/00 cumulative recap no longer mislabels M6's panel components as "REST Countries" (descoped, D7); M11/00 nav counter `12 of 12 → 11 of 12`; added the `viewChild.required` gloss at its true first use (M5/08), since the main pass had only glossed `viewChild` later at M6/05. |
| 2026-07-13 | Audit-fix pass (M0–M4, M6, M7, M9–M11 + foundation/PLAN/README) | Assorted minor/low audit findings (see decision-log) | — | Applied the non-major audit fixes: first-step nav `prev → —` (all 01_*.md); codified the `[Overview]` nav anchor as house style in conventions.md (+ fixed the PLAN template); M4/08 renders full `app.routes.ts` (+ M4/07 reconciled — route already exists since M0); M0/07 same-commit bundle banner; M10/09 bundle count 3→4; M11/00 cumulative artifacts inventory; new global **[troubleshooting.md](../troubleshooting.md)** (PLAN §6); replaced the author's real email with `contact@example.com` (M2/06 + verify); EarthViewMusic cosmetic-brand notes (M7/07, M9/13, M10/17); build-only gate rationale (M1/04,06,08); scope-creep forward-looking notes + dead `toggleHeatMode` note; R1 glosses (PKCE M0/09, CSRF M1/01 +glossary, watermark M3, viewChild M6/05, ::ng-deep M6/08, optimistic-patch M7/03, recordingKey M10/01); R9 arrow-chains → lists (M4/06, M4/07, M11/10); R6 OrbitControls defaults declared (M4/03+04); zone.js gate gains a PowerShell path (M0/00+15). **Touched milestones need re-verification (never mark ✅ without a Done-when run by hand).** |
| 2026-07-13 | M3/06, M5/06, M9/04, M10/05, M3/08+M10/18 verify | `LikedIndex.clear()` was only *defined* in M10 step 05 | It's first *called* in M5 `GlobeStore.clearData()` (and again M9/M11) → the M5 `npm run build` gate fails on a forward reference (worse than the reported M9) | Moved `clear()` into M3 step 06 (beside `commit`) so the whole store surface exists before any caller; reframed M10/05 as "two new relink/dedup mutations, `clear` already built in M3"; reordered + synced both verify copies; added R10 guards in M3/06 and M5/06. |
| 2026-07-13 | `foundation/glossary.md` (39 deep-links from 28 files) | README claimed "0 dead links" but every `glossary.md#…` deep-link fell to the file top | Terms were bullets (`- **term** —`) with no heading/anchor, so no GitHub slug existed to resolve to | Promoted all 24 terms to `### headings` whose GitHub slugs match the linked short forms; repointed the one long-form ISRC link (M10/01) to `#isrc`. Verified: all 21 distinct anchors now resolve, 0 unresolved. |
| 2026-07-13 | M6 (00 overview, 03 country-hover, 05 log-terminal, 07 globe-store-grow, 09 unplaced-artists, 11 verify) | The library feature / `/library/artist/:id` route / master table / inline-picker relocation were attributed to **M9** | The library console is **M10**; M9 is explore filters + boot-sync + the Actions page | Retargeted every library-console reference M9 → M10 (7 spots incl. verify mirrors); left filters/sync/Actions/aggregate references at M9; split the 07 scope note into M9 (filters/Actions) + M10 (library write-back). |
| 2026-07-13 | M5/00 + M5/10 "Deterministic tie-break" gate | "Click Recalculate → same ISO" was said to prove the `ORDER BY ?iso` determinism | `recalculate()` reuses already-resolved countries (never re-queries a placed artist), so it only proves persistence stability | Reworded both gates to force a genuine re-resolution: `localStorage.removeItem('evm.origins')` → reload → full scan, then confirm the same alphabetically-first ISO. |
| 2026-07-13 | M5/06 step-3 signal list | Claimed "the public signals the page reads (step 09)" and listed **16** | Step 09 binds **8** (`heat`, `total`, `resolvedCount`, `failedCount`, `pendingCount`, `hasData`, `isResolving`, `dateRange`); `maxHeat` feeds the canvas; the 7 aggregates are forward-looking (M6+) | Split the list into "page reads 8 (step 09)", "`maxHeat` → canvas ramp (step 08)", and "forward-looking aggregates". |
| 2026-07-13 | M8/00,05,06,07,12,13 + M9/00,11,15 + M11/00,01,03,04,07,11 | M8/05 built the whole marker system (6 non-plane glyphs, land-vehicle→boat terrain swap, sprite mirroring, `setAccent`/`setMarkerColor`/`setMarker`, the `MarkerKind` union) — none of which M8 ever exercises (it only shows a fixed plane) | M8 needs only the plane; the marker system belongs to M11's settings panel, the milestone that actually picks an icon (gold-plating, MAJOR) | Relocated the marker engine M8→M11. M8's `flight-layer` is now plane-only (6-arg ctor, no marker setters/glyphs); the M8 renderer/canvas/page dropped the `markerIcon` chain. `MarkerKind` is **born in** `appearance-cache` (M11/01); the flight layer regrows the full engine (8-arg ctor, glyphs, terrain/mirror, live colours) in M11/03, before the renderer that drives it. Reconciled M9's carried-forward `markerIcon` page binding + every "moves in M11" reference. |

## Session log
- 2026-07-10 — Plan approved (defaults for S1–S6, R1–R7). Scaffolded skeleton + foundation docs + 12 milestone overviews.
- 2026-07-10 — Drafted M0–M7 (parallel subagents, dependency-ordered waves; M5 hit one API drop and was relaunched clean). M6 surfaced two plan-vs-code conflicts → resolved: REST Countries descoped (D7), heat-mode toggle added (D8). Paused after M6 at user request.
- 2026-07-10 — Resumed. Drafted M8→M11 serialized (each extends the shared hot files; each read the prior milestone's checkpoint). M10 was interrupted by a session restart at step 07/18 and resumed in place to completion. **ALL 12 MILESTONES NOW DRAFTED.** Final QA: **140 step files, 0 dead links, 0 nav-line issues, every milestone has overview + verify.** Open audit items captured (scratchpad `audit-items.md`): #2 forward-ref `LikedIndex.clear()` in M9 before M10 introduces it; #1 naming sweep (~54 `earthviewmusic` strings → guide name); hot-file monotonicity check; forward links M6/M8 → `/library/artist/:id`. **Drafting phase complete; whole-guide audit is the next agent-side step (not yet run).**
- 2026-07-13 — **Audit-fix pass (report-issue).** BLOCKER: `LikedIndex.clear()` was defined in M10/05 but first *called* in M5 `GlobeStore.clearData()` → M5's build gate fails on a forward reference (broader than the reported M9). Root cause: the store's public surface was split across milestones. Fix: defined `clear()` in M3 step 06 (beside `commit`), reframed M10/05 as the two relink/dedup mutations only, reordered + synced the M3/08 and M10/18 verify copies, added R10 guards at M3/06 + M5/06, and corrected the M3/00 + M10/18 framing. Milestones M3/M5/M9/M10 flagged ⚠ re-verify.
- 2026-07-13 — **Audit-fix pass (gold-plating, MAJOR).** M8/05 built the entire marker system (6 non-plane glyphs, terrain boat-swap, sprite mirroring, live colour setters, the `MarkerKind` union) that only M11's settings panel uses — M8 only ever shows a fixed plane. Fix (FULL RELOCATION): trimmed M8's `flight-layer.ts` to a plane-only overlay (6-arg ctor, no marker setters/glyphs) and dropped the `markerIcon` chain from the M8 renderer/canvas/page; `MarkerKind` is now **born in** `appearance-cache` (M11/01); the flight layer regrows the full marker engine in M11/03 (folded in, before the renderer that constructs + drives it — no renumber). Reconciled M9's carried-forward `[markerIcon]` page binding (11 + verify) and every "moves in M11" reference. M8 + M11 flagged ⚠ re-verify. Not build-verified.
- 2026-07-13 — **Audit-fix pass (remaining majors + pedagogy).** Glossary: promoted all 24 terms to `###` headings so every `glossary.md#slug` deep-link resolves (was 39 dead links from bullets). M6: retargeted the library-console / `/library/artist/:id` route references from M9 → M10 (M9 keeps filters/sync/Actions). M5: reworded the "deterministic tie-break" gate to force a fresh resolve (clear `evm.origins` → reload → scan) and split the M5/06 signal list into "page reads 8" vs forward-looking. M10/18 + M11/11: softened the "authoritative copy of every file" claims to match what's rendered. three.js pedagogy: defined the Mesh = Geometry + Material triad + MeshPhong/MeshBasic/SphereGeometry (M4/03,04) and LineDashedMaterial/`computeLineDistances`/CanvasTexture/ConeGeometry (M8/05). **Confirmation re-audit: PASS** — no residual blocker/major/warning; only pre-existing template placeholders remain.
- 2026-07-13 — **Minor/low audit-fix replay verified + full re-audit (no content edits).** Checked the minor/low replay list (nav `[Overview]` house style, first-step `prev → —`, M4/07-08 confirm-not-add + full routes, M0/07 & M10/09 bundle banners, M11/00 cumulative inventory, global troubleshooting.md, `contact@example.com` email redaction, EarthViewMusic cosmetic notes, M1/04-06-08 build-only rationale, scope-creep + `toggleHeatMode` notes, all R1 glosses, R9 arrow-chains→lists, R6 OrbitControls defaults, zone.js PowerShell gate): **all 15 items already present** from the 2026-07-13 fix pass — nothing to edit. Ran a fresh whole-guide `/audit-guide` (5 parallel milestone-group subagents + orchestrator global sweeps). **Verdict: PASS-WITH-WARNINGS, 0 BLOCKERs.** Surfaced 2 pre-existing MAJORs NOT in the replay scope + 6 WARNINGs + ~15 MINORs. **User chose "fix everything"; all applied 2026-07-13 (see Drift log).** (1) **M5/06** dead decision-log link `triexplore`→`tripexplore` fixed; (2) **M9/01** duplicate `playlist.ts` reconciled (dropped from create/New; noted "exists since M7") across 01/00/15. WARNINGs fixed: brand flip explained + `index.html <title>` consistency instruction (M1/05) + M1/10 nav-removal note; M4/00 inventory `app.routes.ts` modified→unchanged; M2/05+07 deferral notes (`anyLimited`/`clear` = convenience affordances, `maxRetryAfterMs`→M9); M5/06 `maxHeat`→forward-looking M6; M6/02 legend "can't drift" claim softened; M11/03 bundle banner. ~18 MINORs fixed (bundle banners, REST-Countries descope refs, stack.md OS row, marker re-attributions, Create→Replace, M7 link anchors, M2/00 aimd anchor, M6 hours-precision note, M11 verify numbering, M8/00 `(#)` links). Pedagogy via clarify-step (M4/03 ResizeObserver + atmosphere/light/FOV defaults; M4/04 lerp; M8/05 tunables) — prose-only, no byte-matched code touched. Milestones stay 📝 ⚠ re-verify (unchanged — build-verify by hand still pending).
